{
  "name": "buildtrack-root",
  "version": "1.0.0",
  "description": "Root workspace command helper for BuildTrack MEAN Stack application",
  "scripts": {
    "backend": "npm run start --prefix backend",
    "frontend": "npm run start --prefix frontend -- --port 4200",
    "start": "npx --yes concurrently \"npm run backend\" \"npm run frontend\"",
    "dev": "npx --yes concurrently \"npm run backend\" \"npm run frontend\"",
    "build": "npm run build --prefix frontend"
  }
}
